identity:
  name: "get_models"
  author: "yt-koike"
  label:
    en_US: "Get Models"
description:
  human:
    en_US: "Get the list of available local models"
  llm: "Get the list of available local models"
parameters:
  # No Parameters Required
extra:
  python:
    source: tools/get_models.py
output_schema:
  type: object
  properties:
    models:
      type: array[string]
      label:
        en_US: Models
