identity:
  name: "get_version"
  author: "yt-koike"
  label:
    en_US: "Get Version"
description:
  human:
    en_US: "Get the version of the ollama on the server."
  llm: "Get the version of the ollama on the server."
parameters:
  # No Parameters Required
extra:
  python:
    source: tools/get_version.py
output_schema:
  type: object
  properties:
    version:
      type: string
      label:
        en_US: Version
